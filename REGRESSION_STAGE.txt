CARRERA-HUB REGRESSION STAGE 4R5
================================
Name: Concurrent Recovery Synchronization / Race Hardening
Parent baseline: Stage 4R4 - Dashboard Resize & Table Layout Hardening

Fixes:
- Canonical single numeric PID shared by watchdog and strict verifier.
- Per-package single-flight recovery; duplicate workers are rejected.
- Single recovery yields when global recovery owns the packages.
- Watchdog snapshots all packages first, commits crash states second, then
  starts recovery workers, preventing interleaving during simultaneous crash.

Stage 4R4 invariants remain unchanged: resize-safe manual Rich Live refresh,
one-time startup terminal reset, 60-column dashboard, tier 1-3 semantics,
30-second minimum recovery delay, and strict package-isolated verification.

Regression test:
Force-close both clones together. Both should recover to Farming and remain
stable for multiple watchdog cycles unless a real package process loss occurs.
